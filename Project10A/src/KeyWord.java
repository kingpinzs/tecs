/**
 * Jack language keywords.
 * 
 * @author John Ness
 */
public enum KeyWord {

	// class
	CLASS, STATIC, FIELD, INT, CHAR, 
	CONSTRUCTOR, FUNCTION, METHOD, 
	VAR, VOID, TRUE, FALSE, NULL, THIS, 
	LET, RETURN, WHILE, IF, ELSE, BOOLEAN
}
